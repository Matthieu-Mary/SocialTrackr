// Point d'entrée TypeScript pour le backend
console.log("Configuration TypeScript réussie !");

// Import des dépendances nécessaires
import express from "express";

// Cette ligne est juste pour vérifier que TypeScript reconnaît bien le type
const app: express.Application = express();

export default app;
